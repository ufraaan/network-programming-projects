# tcp echo server & client

a [tcp](https://en.wikipedia.org/wiki/Transmission_Control_Protocol) echo server that sends back whatever it receives.

## build

```bash
make
```

run server first, then send a message from the client.
