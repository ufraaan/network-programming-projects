# rpc addition service

an [rpc](https://en.wikipedia.org/wiki/Remote_procedure_call)-based service that adds two numbers.

## build

```bash
make
```

run server with `./add_server -F`, then send two numbers from the client.
