# rpc daytime service

an [rpc](https://en.wikipedia.org/wiki/Remote_procedure_call)-based service that returns the current date and time.

## build

```bash
make
```

run server with `./day_server -F`, then connect with the client.
