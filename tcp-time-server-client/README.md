# tcp time server & client

a [tcp](https://en.wikipedia.org/wiki/Transmission_Control_Protocol) server that returns the current time when a client connects.

## build

```bash
make
```

run server first, then connect with the client.
