# rpc echo service

an [rpc](https://en.wikipedia.org/wiki/Remote_procedure_call)-based echo service.

## build

```bash
make
```

run server with `./echo_server -F`, then send a message from the client.
